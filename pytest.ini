[pytest]
# The suite imports `app.*` absolutely, so the repo root must be importable.
# `python -m pytest` gets this for free; a bare `pytest` (as CI runs) does not.
pythonpath = .
testpaths = app/tests
